import { CheckRoleDirective } from './check-role.directive';

describe('CheckRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckRoleDirective();
    expect(directive).toBeTruthy();
  });
});
